<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'SQL Probe', {action: 'index'}, class: 'navbar-brand' %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= params[:sql_filter].blank? && 'active' %>">
          <%= link_to 'View All', {action: 'index'} %>
        </li>
        <li class="<%= params[:sql_filter].present? && 'active' %>">
          <%= link_to 'Only Changes', {action: 'index', sql_filter: 'insert,update,delete'.html_safe} %>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="navbar-form">
          <% if SqlProbe.listening? %>
            <%= button_to "Stop listening", stop_path, disabled: !SqlProbe.listening?, class: 'btn btn-default' %>
          <% else %>
            <%= button_to "Start listening", start_path, disabled: SqlProbe.listening?, class: 'btn btn-default' %>
          <% end %>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            Clear SQL Data &hellip; <span class="caret"></span>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to "Delete all SQLs from: #{SqlProbe.output_base_dir}", reset_path %></li>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
